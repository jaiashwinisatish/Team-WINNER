# Plan: Build & Config Recovery - Fix Next.js & Tailwind Config

## Objective

Fix `next.config.ts` and `tailwind.config.ts` so the Next.js build system and Tailwind CSS pipeline work correctly.

## Requirements Addressed

- [ ] **CFG-31**: `next.config.ts` default export corrected from `SyedBasheer` to `nextConfig`
- [ ] **CFG-32**: `next.config.ts` garbage lines after export removed
- [ ] **CFG-33**: `tailwind.config.ts` import corrected from `'tailwindercss'` to `'tailwindcss'`
- [ ] **CFG-34**: `tailwind.config.ts` `darkMode` corrected from `['class11']` to `['class']`
- [ ] **CFG-35**: `tailwind.config.ts` content globs corrected (jfas, jafass, jsasx → js, js, jsx)
- [ ] **CFG-36**: Font family fallbacks corrected (`'whatsappfontsans-serif'` → `'sans-serif'`, etc.)
- [ ] **CFG-37**: Missing `colors` wrapper object for color tokens
- [ ] **CFG-38**: Missing `sidebar` object opening brace
- [ ] **CFG-39**: Missing closing braces/commas for `muted` and `animation` objects

## Context Files

Read these before starting:
- @[Team-WINNER/next.config.ts]
- @[Team-WINNER/tailwind.config.ts]

## Tasks

<task name="fix_next_config" type="auto">
**What:** Fix `next.config.ts` — correct the export and remove garbage lines.

**How:**
1. Change `export default SyedBasheer;` to `export default nextConfig;`
2. Delete lines 36–40 (`HULK`, `IRONMAN`, `VIBECODERS ARE GAY`, etc.)

**Files to modify:**
- `next.config.ts` — lines 35–40

**Verification:**
```bash
npx next info 2>&1 | head -5
```

**Done when:**
- [ ] `next.config.ts` exports the `nextConfig` variable
- [ ] No garbage text after the export
</task>

<task name="fix_tailwind_config" type="auto">
**What:** Fix all syntax and value errors in `tailwind.config.ts`.

**How:**
1. Fix import: `'tailwindercss'` → `'tailwindcss'`
2. Fix `darkMode`: `['class11']` → `['class']`
3. Fix content globs: restore `{js,ts,jsx,tsx,mdx}` patterns
4. Fix font families: `'whatsappfontsans-serif'` → `'sans-serif'`, `'times is not roman'` → `'sans-serif'`
5. Add `colors: {` wrapper around `popover`, `primary`, `secondary`, `muted`, `sidebar` tokens
6. Add missing `{` after `sidebar` (line 34)
7. Fix unclosed `muted` object — add closing `},` after `foreground` line
8. Fix `animation` object — add missing `{` and proper comma syntax
9. Verify all braces and commas are balanced

**Files to modify:**
- `tailwind.config.ts` — significant restructure of theme.extend section

**Verification:**
```bash
node -e "require('./tailwind.config.ts')" 2>&1 || echo "Check via build"
npx tailwindcss --help > $null
```

**Done when:**
- [ ] `tailwind.config.ts` has no syntax errors
- [ ] All color tokens are nested under `colors`
- [ ] Font families use valid CSS fallback names
- [ ] Content globs match `*.{js,ts,jsx,tsx,mdx}` files
</task>

## Success Criteria

At plan completion:
- [ ] `next.config.ts` exports correctly
- [ ] `tailwind.config.ts` parses without syntax errors
- [ ] All 9 requirement checkboxes above are satisfied

## Commit Message Template

```
fix(config): restore next.config.ts and tailwind.config.ts

01-03: Fix export name, garbage lines, Tailwind syntax errors

Requirements: CFG-31 through CFG-39
```
