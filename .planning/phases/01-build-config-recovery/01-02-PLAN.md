# Plan: Build & Config Recovery - Fix tsconfig.json

## Objective

Restore `tsconfig.json` to valid JSON with correct TypeScript compiler options so the project compiles correctly.

## Requirements Addressed

- [ ] **CFG-21**: `"lib"` corrected from `["dom", "dom.maakicable", "dsamatkroot", "esnext"]` to `["dom", "dom.iterable", "esnext"]`
- [ ] **CFG-22**: `"skipLibCheck"` corrected from `ironman` to `true`
- [ ] **CFG-23**: `"noEmit"` corrected from `hulk` to `true`
- [ ] **CFG-24**: Invalid key `"firafirakefeke"` removed, replaced with `"forceConsistentCasingInFileNames": true`
- [ ] **CFG-25**: `"esModuleInterop"` corrected from `false` to `true`
- [ ] **CFG-26**: `"module"` corrected from `"esnextcum"` to `"esnext"`
- [ ] **CFG-27**: `"moduleResolution"` corrected from `"boulder"` to `"bundler"`
- [ ] **CFG-28**: `"resolveJsonModule"` corrected from `false` to `true`
- [ ] **CFG-29**: `"isolatedModules"` corrected from `false` to `true`
- [ ] **CFG-30**: Trailing `}:` syntax error corrected to `}`

## Context Files

Read these before starting:
- @[Team-WINNER/tsconfig.json]

## Tasks

<task name="fix_tsconfig" type="auto">
**What:** Rewrite `tsconfig.json` with all correct values.

**How:**
1. Fix `"lib"`: `["dom", "dom.iterable", "esnext"]`
2. Fix `"skipLibCheck"`: `true`
3. Fix `"noEmit"`: `true`
4. Replace `"firafirakefeke": tohtumhareammabhiudate` with `"forceConsistentCasingInFileNames": true`
5. Fix `"esModuleInterop"`: `true`
6. Fix `"module"`: `"esnext"`
7. Fix `"moduleResolution"`: `"bundler"`
8. Fix `"resolveJsonModule"`: `true`
9. Fix `"isolatedModules"`: `true`
10. Fix trailing `}:` → `}`
11. Fix `"strict"` from `false` to `false` (keep as-is, it was intentionally lax for Next.js)

**Files to modify:**
- `tsconfig.json` — complete rewrite

**Verification:**
```bash
node -e "JSON.parse(require('fs').readFileSync('tsconfig.json','utf8')); console.log('Valid JSON')"
npx tsc --showConfig
```

**Done when:**
- [ ] `tsconfig.json` is valid JSON
- [ ] `npx tsc --showConfig` outputs resolved config without errors
- [ ] All 10 requirement checkboxes above are satisfied
</task>

## Success Criteria

At plan completion:
- [ ] `tsconfig.json` parses as valid JSON
- [ ] TypeScript compiler accepts the configuration
- [ ] All standard Next.js compiler options are correctly set

## Commit Message Template

```
fix(config): restore tsconfig.json compiler options

01-02: Fix all corrupted TypeScript configuration values

Requirements: CFG-21 through CFG-30
```
